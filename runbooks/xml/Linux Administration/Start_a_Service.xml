<joblist>
  <job>
    <context>
      <options preserveOrder='true'>
        <option enforcedvalues='true' name='service' required='true' value='nginx' values='mysql,nginx' valuesListDelimiter=','>
          <label>Linux Service</label>
        </option>
      </options>
    </context>
    <defaultTab>output</defaultTab>
    <description>Check a Linux service status and start if it's down.</description>
    <dispatch>
      <excludePrecedence>false</excludePrecedence>
      <keepgoing>true</keepgoing>
      <rankOrder>ascending</rankOrder>
      <successOnEmptyNodeFilter>false</successOnEmptyNodeFilter>
      <threadcount>1</threadcount>
    </dispatch>
    <executionEnabled>true</executionEnabled>
    <group>Administration/Linux</group>
    <id>acc2844d-28be-4cf4-9420-ca9b139ee537</id>
    <loglevel>INFO</loglevel>
    <name>Start a Service</name>
    <nodeFilterEditable>true</nodeFilterEditable>
    <nodefilters>
      <filter>name: node00</filter>
    </nodefilters>
    <nodesSelectedByDefault>true</nodesSelectedByDefault>
    <plugins />
    <scheduleEnabled>false</scheduleEnabled>
    <sequence keepgoing='true' strategy='node-first'>
      <command>
        <description>Get the service status using pgrep</description>
        <fileExtension>.sh</fileExtension>
        <plugins>
          <LogFilter type='key-value-data'>
            <config>
              <invalidKeyPattern>\s|\$|\{|\}|\\</invalidKeyPattern>
              <logData>true</logData>
              <regex>^(status)\s*=\s*(.+)$</regex>
            </config>
          </LogFilter>
        </plugins>
        <script><![CDATA[# get the status using pgrep
if pgrep "@option.service@" >/dev/null; then
  echo "status=running"
else
  echo "status=stopped"
fi
]]></script>
        <scriptargs />
        <scriptinterpreter>/bin/bash</scriptinterpreter>
      </command>
      <command>
        <description>Print the service current status</description>
        <exec>echo "the ${option.service} service is ${data.status}"</exec>
      </command>
      <command>
        <description>Start the service if is stopped</description>
        <fileExtension>.sh</fileExtension>
        <script><![CDATA[# start the service if the stauts is "stopped"
if [ @data.status@ = 'stopped' ]; then
    echo "starting @option.service@..."
	sudo /etc/init.d/nginx start
else
    echo "System is inactive, nothing to do."
fi

# all done.
echo "Done."
]]></script>
        <scriptargs />
        <scriptinterpreter>/bin/bash</scriptinterpreter>
      </command>
    </sequence>
    <uuid>acc2844d-28be-4cf4-9420-ca9b139ee537</uuid>
  </job>
</joblist>